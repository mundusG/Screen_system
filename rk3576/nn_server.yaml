name: nn_server
enable: true
# absroot: yes
entrypoint: ../../../service/nn_server/bin/nn_server
isdup: true
# dupfile: /var/log/dposter.txt
command:
  - '-c'
  - 'nn_server.conf'
environment:
  - version=v1
  - LD_LIBRARY_PATH=/oem/usr/lib/
